//! Sidebar component

use leptos::*;

#[component]
pub fn Sidebar() -> impl IntoView {
    view! {
        <aside class="sidebar">
            <nav>
                // Navigation items will be rendered dynamically based on metadata
            </nav>
        </aside>
    }
}
